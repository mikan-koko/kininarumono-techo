# DESIGN.md — 気になるモノ手帖 デザイン規約

UIを触る前に読むこと。トークンの実体は `public/css/style.css` の `:root`。**このファイルとCSSがズレたら、CSSを正とした上でこのファイルを直す。**

---

## 1. 方向性

**ポップ、ただし規律を持って。**

紙色の上に「墨の輪郭 + ぼかさないハード影」を全パーツに通した**ステッカーの言語**で統一する。色は**カテゴリの識別だけ**に使い、面で暴れさせない。

参照したのは以下2サイト。ピクセルではなく原理を抽出している。

| | 抽出した原理 |
|---|---|
| ikiiki-being.com | 主色1色をほぼ全面に効かせる規律（実測で主色が957箇所）／ピル形状の徹底（`border-radius:9999px` が37箇所）／巨大な和文ディスプレイ／YakuHanJPによる約物最適化／紙色のベース |
| sou-group.jp | 温かい黒（#1A1311）を地にする／カードごとに色を配分／上部を大きく丸めたアーチ型メディア／黒ピル＋丸アローのCTA／縁取りタグ／フラットな装飾図形 |

---

## 2. 色

```css
--paper:#FBF7EF;  --paper-2:#FFFDF8;   /* 紙 */
--ink:#17130E;    --ink-2:#4B433A;     /* 墨・本文 */
--muted:#6E675C;  --line:#E4DACA;
```

カテゴリ色は**3つ1組**。用途を混ぜないこと。

| カテゴリ | `--c`（面） | `--c-deep`（文字） | `--c-soft`（淡面） |
|---|---|---|---|
| gadget ガジェット | `#5B2BEB` | `#4A1FCC` | `#EDE8FE` |
| interior インテリア | `#10B6A8` | `#067A72` | `#DFF5F3` |
| goods 雑貨 | `#FF4B3E` | `#C92A1B` | `#FFE8E5` |
| fashion ファッション | `#FF9E1B` | `#8F5200` | `#FFF0DC` |

アクセント: `--pink:#FF3D9A` / `--pink-deep:#C4166B` / `--yellow:#FFC61A`

### なぜ3つに分かれているか

**`--c` を文字色や文字の背景に使うと WCAG AA を割る。** 白文字×アンバーは約2:1、白文字×ティールは約2.5:1しかない。実際に一度そのバグを作り込んでいる。

- 面・図案・影・装飾 → `--c`
- **文字色**（白地の上）→ `--c-deep`
- **文字の背景**（白文字を乗せる、チップの選択状態など）→ `--c-deep`
- タグの淡い下地（`--c-deep` の文字を乗せる）→ `--c-soft`

現状の実測値は最低4.58（`.card__cat`）。**新しい配色を足したら必ず測る。**

---

## 3. タイポグラフィ

```css
--display:"YakuHanJP","Dela Gothic One","Zen Maru Gothic",sans-serif;  /* h1,h2,ワードマーク */
--jp:"YakuHanJP","Zen Maru Gothic",system-ui,sans-serif;               /* 本文・UI・h3 */
--latin:"Outfit",var(--jp);                                            /* 欧文・数字 */
```

- **Dela Gothic One は 400 しか無い。** `font-weight:800` などを指定すると合成太字で汚れる。h1/h2 は `font-weight:400` 固定。
- 字幅が広いので `clamp()` の上限は控えめ（ヒーロー `clamp(34px,7.4vw,92px)`）。書体を替えたら必ず再調整する。
- `body` に `font-feature-settings:"palt" 1`。YakuHanJP をスタックの先頭に置いて和文約物（「」、。）だけ半角に差し替えている。
- **句点を装飾要素の外に置かない。** 約物が半角化して宙に浮いた丸に見える。ヒーローでは `<span class="pop">アガる。</span>` のようにステッカー内に入れている。

---

## 4. 形とステッカー言語

```css
--r-card:26px;    /* カード・パネル */
--r-arch:68px;    /* カードメディアの上辺アーチ */
--bd:2.5px solid var(--ink);
--pop:5px 6px 0 var(--ink);      /* ハード影（ぼかさない） */
--pop-lg:9px 11px 0 var(--ink);  /* ホバー時 */
--ease:cubic-bezier(.2,.9,.25,1.15);
```

- **影は絶対にぼかさない。** `blur` を入れた瞬間に汎用SaaSの見た目に落ちる。
- ボタン・タグ・チップは `border-radius:999px` のピル。
- カードは内側に余白を持つ「台紙」構造。メディアは上辺だけ大きく丸めたアーチ。
- ホバーは `translate(-4px,-4px)` + 影を伸ばす。沈むのではなく浮かせる。

---

## 5. 図案（SVGモチーフ）

`index.html` 冒頭にインラインした `<symbol>` スプライト。現在29種。`<use href="#m-xxx">` で参照する。

### 描画規約

- `viewBox="0 0 64 64"` 固定
- 本体 = 紙白（`.mo-b`） / 輪郭 = 墨 `stroke-width:3`（`.mo`） / 差し色 = イエロー（`.mo-a`） / 塗りつぶし = 墨（`.mo-i`）
- `stroke-linecap` `stroke-linejoin` は `round`
- **紫・ティール・コーラル・アンバーのどの面に載せても成立する配色にする。** カテゴリ色の上に置かれるため。

### 質感

- 面には**ハーフトーンの網点**（`.card__media--blank` の2層 `radial-gradient`）
- モチーフは `<use>` を2枚重ね、下層（`.card__motif-ghost`）をカテゴリ濃色一色に潰して少しずらし、**オフセット印刷の版ズレ**に見せている
- 色の差し替えは CSS カスタムプロパティ経由。**クラスセレクタは `<use>` のシャドウツリーに届かない**が、カスタムプロパティは継承される。この仕組みのおかげで29図案を描き直さずに一括で色を変えられる

### 隣接重複を避ける

同じ図案が隣り合うカードに並ぶと手抜きに見える。製品が実際に違うなら描き分ける（例: セブンチェア / Yチェア、アアルトの波形 / フローラ、トレイル靴 / ランニング靴）。
同一ブランドの同一商品ラインなら同じ図案でよい（SWAGの歯磨き粉2本、表現社のメモ2種）。

---

## 6. 動き

- ヒーロー = 読み込み時の段階表示（`animation-delay` で 0.04s 刻み）。**散らすより1回の演出にまとめた方が効く。**
- カード = `animation-timeline: view()` によるスクロール連動。`animation-fill-mode: backwards` にして範囲外では通常スタイルに戻し、ホバーの変形を殺さない。未対応ブラウザは IntersectionObserver にフォールバック（`main.js` の `sda` 判定）。
- ボタン = ポインタに吸い寄せる（マグネット）
- カード = 傾き。JSは `--rx` `--ry` を渡すだけにして、CSS側のホバー移動と合成させる
- 絞り込み = View Transition（`vtBusy` ガード必須。AGENTS.md 6章参照）
- マーキー = 掲載ブランドを流す帯。`PICKS` から自動生成なので商品を足せば自動で増える

**すべて `@media (prefers-reduced-motion: reduce)` で止める。**

---

## 7. 禁止事項

- ❌ **ぼかした影**（`box-shadow` に blur を入れる）
- ❌ **紫〜青のグラデーション**、グラデーションを装飾目的で使うこと
- ❌ **Inter / Roboto / システムフォント**を見出しに使う
- ❌ カテゴリ色（`--c`）を文字色や文字の背景に使う（AA違反になる）
- ❌ 角丸カードを3列並べただけのヒーロー
- ❌ 意味の無い連番（`No.01` は32件の通し番号として機能しているので可。順序に意味が無い箇所に振らない）
- ❌ 全要素に散らしたアニメーション
- ❌ セール価格を `price` に書く（期間終了で嘘になる）
- ❌ 単色ベタ塗りの背景（紙のグレイン・方眼ドット・網点で質感を入れる）

---

## 8. レイアウト

- 最大幅 `--maxw:1160px`
- グリッドは3カラム（`grid-auto-flow:dense`）。860px以下で2カラム、560px以下で1カラム
- 写真ありのカードは2カラム占有のフィーチャー。誌面にリズムを出すための仕掛けで、`img` の有無で自動判定される
- 狭い画面では装飾（`.blob`）が本文に重なるので、560px以下ではヒーロー横の1つだけ残して他は消す。**装飾を足したら重なりを実測で確認すること**
